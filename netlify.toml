[build]
  base = "client"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA: React Router paths (e.g. /admin, /booking) must serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
